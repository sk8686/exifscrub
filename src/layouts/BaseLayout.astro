---
import Header from '../components/Header.astro';

interface Props {
  title: string;
  description: string;
  canonicalURL?: string;
}

const { title, description, canonicalURL } = Astro.props;
const currentPath = Astro.url.pathname;
---

<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta name="description" content={description} />
    <title>{title}</title>
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link rel="canonical" href={canonicalURL} />
    <meta name="robots" content="index, follow" />
    <meta property="og:title" content={title} />
    <meta property="og:description" content={description} />
    <meta property="og:type" content="website" />
    <meta name="twitter:card" content="summary" />
    <meta name="twitter:title" content={title} />
    <meta name="twitter:description" content={description} />
    <script type="application/ld+json" set:html={JSON.stringify({
      "@context": "https://schema.org",
      "@type": "WebApplication",
      "name": "EXIF Remover",
      "description": description,
      "applicationCategory": "UtilitiesApplication",
      "operatingSystem": "Any",
      "offers": {
        "@type": "Offer",
        "price": "0",
        "priceCurrency": "USD"
      },
      "featureList": [
        "Remove EXIF metadata from photos",
        "Remove GPS location data",
        "Remove camera information",
        "Batch processing",
        "100% local browser processing"
      ]
    })} />
  </head>
  <body class="min-h-screen flex flex-col bg-[var(--color-bg)]">
    <Header currentPath={currentPath} />
    <main class="flex-1">
      <slot />
    </main>
    <footer class="border-t border-[var(--color-border)] bg-[var(--color-bg-alt)] py-8 mt-12">
      <div class="max-w-5xl mx-auto px-4 text-center text-sm text-[var(--color-text-muted)]">
        <p class="mb-2">🔒 Your files never leave your device. All processing happens locally in your browser.</p>
        <p>&copy; {new Date().getFullYear()} EXIF Remover. Free &amp; open source.</p>
      </div>
    </footer>
  </body>
</html>
